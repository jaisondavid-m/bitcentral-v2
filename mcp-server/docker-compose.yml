version: '3.8'

services:
  mcp-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bitcentral-mcp-server
    restart: always
    ports:
      - "8081:8081"
    env_file:
      - .env
    environment:
      - PORT=8081
      - MCP_TRANSPORT=sse
